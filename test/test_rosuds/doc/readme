1. build this "${ros_comm}" package and use it as runtime environment

  create directory for ROS catkin workspace
  $ mkdir -p ros_catkin_ws/src

  put "${ros_comm}" into ros_catkin_ws/src and then build
  $ cd ros_catkin_ws
  $ catkin_make               // Use '-j4' option at hikey machine.
  $ source devel/setup.bash

  Note: it can also use 'catkin_make_isolated' to replace with 'catkin_make'.
      (But not use with '--install', because all tests about UDS will not be installed.)


2. run test for rosuds
  Before running test, please make sure run 'ROS_UDS_EXT_ENABLE=on roscore' first.

  $ ./src/"${ros_comm}"/test/test_rosuds/run_test.sh   // generate a result file at 'log' directory


3. check the log, make sure all tests passed

  $ grep RESULT log/* | grep NG
  $ (show nothing)
